{
  "name": "Xray",
  "version": "0.1.0",
  "main": "./out/main_process/main.js",
  "scripts": {
    "build-ts": "scripts/node_modules/.bin/tsc --project src",
    "watch-ts": "scripts/node_modules/.bin/tsc --project src --watch",
    "install-script-dependencies": "cd scripts && npm install",
    "install": "npm run install-script-dependencies && npm run build-ts"
  },
  "license": "MIT",
  "dependencies": {
    "xray": "../xray_node"
  },
  "devDependencies": {
    "electron": "^1.7.11"
  }
}
