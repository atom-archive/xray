[package]
name = "xray_wasm"
version = "0.1.0"
authors = ["Antonio Scandurra <as-cii@github.com>"]

[lib]
crate-type = ["cdylib"]

[dependencies]
futures = "0.1"
wasm-bindgen = "0.2"

[features]
js-tests = []
